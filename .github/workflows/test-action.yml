name: test-action
on:
  pull_request:
    branches: ["main"]
jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20.x]
    steps:
      - name: Install dependencies
        run: npm i

      - name: Run tests
        run: npm run test
